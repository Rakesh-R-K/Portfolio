import '../globals.css';

export const metadata = {
    title: 'ADMIN - Portfolio Control Panel',
    description: 'Manage your portfolio content',
    robots: { index: false, follow: false },
};

export default function AdminLayout({ children }) {
    return children;
}
